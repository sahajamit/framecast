# .claude/

Project-level Claude Code assets for framecast. Repo-wide agent guidance lives in
`/CLAUDE.md`; this folder holds the pieces that plug into the harness directly.

Conventions when we add things here:

- `commands/` — custom slash commands (e.g. a future `/release` or `/record-demo`)
- `agents/` — custom subagent definitions
- `skills/` — project-local skills

Nothing here yet beyond this note; the folder exists so future sessions know where
these belong.
